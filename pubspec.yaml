name: aimedication_system
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.12.1  # Replace with the latest stable version
  firebase_auth: ^5.5.1  # Replace with the latest stable version
  google_sign_in: ^6.2.2  # Replace with the latest stable version
  cupertino_icons: ^1.0.8
  mongo_dart: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
